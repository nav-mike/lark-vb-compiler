package jlark;

/**
 * Перечисление типов услового перехода.
 * @version 1.0
 */
public enum JVBIfStmtType {
    
    /** If ... Them тип. */
    IF_THEN,
    /** If ... тип. */
    IF_ENDL,
    /** If... односточный. */
    IF_INLINE,
    /** If...Else однострочный. */
    IF_ELSE_INLINE
}
